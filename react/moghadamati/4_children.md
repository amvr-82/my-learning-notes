

<div align="right" >

# 📦 مفهوم `children` در React

## 🧠 `children` چیست؟
در React، `children` یک **prop ویژه** است که به‌صورت خودکار توسط React به هر کامپوننت ارسال می‌شود و شامل **محتوایی است که بین تگ باز و بسته‌ی کامپوننت قرار می‌گیرد**.

---

## ✨ مثال ساده
```jsx
<MyComponent>
  سلام دنیا
</MyComponent>
```

در مثال بالا، مقدار زیر به کامپوننت ارسال می‌شود:

```js
props.children === "سلام دنیا"
```

---

## 🧩 استفاده در کامپوننت
```jsx
function Card({ children }) {
  return (
    <div className="card">
      {children}
    </div>
  );
}
```

### نحوه استفاده:
```jsx
<Card>
  <h2>عنوان کارت</h2>
  <p>این یک توضیح است</p>
</Card>
```

✅ کامپوننت `Card` بدون دانستن محتوای داخلی، آن را رندر می‌کند.  
✅ این کار باعث **Reusable بودن کامپوننت** می‌شود.

---

## 🔥 `children` چه نوع داده‌ای می‌تواند باشد؟
`children` از نوع `ReactNode` است و می‌تواند شامل موارد زیر باشد:

- متن (string)
- JSX
- کامپوننت دیگر
- آرایه‌ای از JSX
- `null` یا `undefined`

---

## ⚠️ اشتباه رایج
❌ نادرست:
```jsx
function Box() {
  return <div>{children}</div>;
}
```

✅ درست:
```jsx
function Box({ children }) {
  return <div>{children}</div>;
}
```

---

## 🧠 استفاده حرفه‌ای با TypeScript
```tsx
type Props = {
  children: React.ReactNode;
};

function Modal({ children }: Props) {
  return <div className="modal">{children}</div>;
}
```

یا:

```tsx
type Props = React.PropsWithChildren<{}>;
```

---

## ✅ جمع‌بندی
> `children` راهی است برای ارسال محتوای دلخواه به داخل کامپوننت  
> بدون وابسته کردن کامپوننت به ساختار داخلی محتوا.

---

✨ با استفاده از `children` می‌توان:
- Layout ساخت
- Wrapper Component طراحی کرد
- UI منعطف و تمیز نوشت

</div>
```

---
